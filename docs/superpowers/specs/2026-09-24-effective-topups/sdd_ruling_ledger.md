# SDD ledger — plan: docs/superpowers/plans/2026-09-24-effective-topups.md

Branch: feat/effective-topups (from docs/effective-topups-spec @ 4ea0814), in place (session configured
to work in the main checkout, no worktree). Spec: docs/superpowers/specs/2026-09-24-effective-topups-design.md.
Tests: pytest is hook-blocked in Bash for every agent -> run via `opencode run` (see plan Global Constraints),
targeted files only (user preference).

## Pre-flight scan

| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T0 ↔ T4 | T0 adds a warn in `mbar_subsample.equilibrated_subsample`'s pymbar_missing path; T4 switches the union builder to the full `equilibrated_subsample` result | compatible; T4 records `status` which will read `pymbar_missing` where pymbar is unusable |
| T1 ↔ T8 | T1 `same_rung_neighbours`, `other_rung_same_centre`, `spatial_neighbour_pairs` in `gareus.layout_neighbours`; T8 imports exactly these | consistent |
| T2 ↔ T6/T7/T8 | T2 policy fields `topups_enabled, topup_target_sigma, topup_weak_overlap, topup_max_fraction, topup_min_effect, topup_max_edge_attempts, topup_throughput_table`; T6/T7/T8 read these names | consistent |
| T2 ↔ T8 | both edit the `topups_enabled` block in `run_scheduled_adaptive_epoch` | T8 replaces T2's one-line default change; sequential, fine |
| T3 ↔ T6/T8 | `wall_hours(steps, n_states, timestep_fs, n_gpus, table)` | consistent |
| T4 ↔ T5/T8 | `meta["subsample_counts_per_state"][str(id)] = {raw, t0, kept, g, status}`; T5 `subsample_counts` reads g, raw, t0, kept; T8 passes `meta.get(...)` | consistent |
| T5 ↔ T6 | `UnionDiagnostics(state_ids, n_k, sigma_kcal, unconverged, inefficiency, edge_overlap, f_kT)`; T6 tests build it with all 7 fields | consistent |
| T6 ↔ T8 | `TopupPlan` incl. `weak_edges_topped`, `sigma_before`; T8 `save_plan`/`load_plan`/`update_after_topup` use them | consistent |
| T7 ↔ T8 | T7 rewires `_weak_edge_touch_counts` to `_edge_is_measured_weak`; T8 deletes `_weak_edge_touch_counts` (score allocator removed) | compatible: gate keeps `_edge_is_measured_weak` |
| **T8 ↔ T9** | T8's tests and `_seedable_patch` import `gareus.topup_seeding` (`load_seed_index`, `SeedMismatchError`), which T9 creates | **order conflict** — see Ruling 1 |
| T5/T6 ↔ T10 | T10 calls `union_diagnostics_from_npz(..., subsample_counts=...)` and `plan_topup(...)` with the rev-2 signatures | consistent |
| T0 self | code vs tests (`pymbar_status` cache + builtins monkeypatch) | consistent |
| T1 self | move + two helpers, tests exercise both | consistent |
| T2 self | malformed-table test expects SystemExit; shim raises SystemExit | consistent |
| T3 self | executed in scratch (rev 2): tests pass | consistent |
| T4 self | fixture writes 200 rows/window under `adaptive/final`; asserts raw==200, 0<=t0<raw, kept<=raw-t0, g>=1 | consistent |
| T5 self | executed in scratch (rev 2): 30/30 with T3/T6/T0 | consistent |
| T6 self | executed in scratch (rev 2) | consistent |
| T7 self | tests call `_edge_is_measured_weak` directly | consistent |
| T8 self | `_drive` monkeypatches `_topup_plan_for_phase`, `_seedable_patch`, `_after_topup_update`; code calls all three through module globals with matching arities; `time` import noted in plan | consistent |
| T9 self | test uses `build_small_simulation`, defined in the same task | consistent |
| T10 self | prose + tests; thresholds (>=4 of 5 wins; <5% on homogeneous) are empirical | risk noted, not a conflict |
| T11 self | helptext test needs underline length == title length | consistent |

Ruling 1: execute Task 9 before Task 8 (order 0,1,2,3,4,5,6,7,9,8,10,11) — T9 has no dependency on T8, T8's tests import T9's module; the plan's own T8 Step 2 already anticipates it — cost if wrong: none (T9 standalone).
Ruling 2: every agent runs tests through `opencode run`, writing its log to a per-task untracked file `atlas_task<N>.log` in the repo root, never committed — pytest is hook-blocked; cost if wrong: an agent wastes turns on a blocked command.
Ruling 3: no aurum2 deploy or env change as part of this plan's execution; the aurum2 jax removal happens separately at the chignolin_9 epoch_002->final boundary (user decision 2026-09-25) — cost if wrong: none for the branch.

## Progress
Task 0: dispatched (base 4ea0814, haiku, agent a9926bea88ed1c81a)
Task 0: review 1 -> spec ✅ (literal), Important #1 plan-mandated: cli.py warning placed in run_double_adaptive_auto_loop only; plain adaptive-production (chignolin_9) bypasses it.
Ruling 4: move/extend the startup warn_if_pymbar_unusable call so it fires once for EVERY adaptive-production entry path (shared point before the window_mode dispatch, or each AP call site) — the spec goal is "loud at startup"; the brief's function pointer was the controller's mistake — cost if wrong: one redundant warning line.
Task 0: minor (deferred): no test asserts the wiring in mbar_subsample (once-per-process) or cli (called on each AP path).
Task 0: fix round 1/5 (wiring addressed, test missing — required once-per-process test not written; 4 duplicated guard blocks; commits 108ceee..9318a06)
Task 0: fix round 2/5 (2 addressed, 0 open; commits 9318a06..c18cb4d)
Task 0: complete (commits 4ea0814..c18cb4d, review clean)
Task 1: review 1 -> 2 Important (both plan-mandated): (a) layout_neighbours imports restraint_sigma from gareus.dashboard.ranking (core->dashboard coupling; cold-import circular crash reproduced); (b) other_rung_same_centre compares raw lambdas while _layout rounds them.
Ruling 5: move restraint_sigma into gareus/math_helpers.py (core; depends only on units), keep a re-export in gareus/dashboard/ranking.py; round lambdas with _CENTRE_DECIMALS in other_rung_same_centre too — spec requires a core allocator free of dashboard imports; one rung identity per module — cost if wrong: one extra re-export line.
Task 1: minor (deferred): same_rung_neighbours omits keys for windows with no partner (consumers must use .get(w, [])); no test for a centre mixing known and NaN lambdas; implementer report's test-count breakdown wrong (4 new, not 5).
Task 1: fix round 1/5 (2 addressed, 0 open; commits f0b007a..51b5b60)
Task 1: complete (commits c18cb4d..51b5b60, review clean)
Task 2: complete (commits 51b5b60..9ff4a73, review clean)
Task 2: minor (deferred): "top-ups disabled (--no-ap-topups)" log line in run_scheduled_adaptive_epoch now prints on the default path too (Task 8 rewrites that block — fix the wording there).
Task 3: complete (commits 9ff4a73..f12d44a, review clean)
Task 4: complete (commits f12d44a..dfd35bf, review clean)
Task 3: minor (deferred): semicolon-joined assignment line in gareus/adaptive/throughput.py.
Task 4: minor (deferred): no test drives the g-fallback branch (verified by inspection: fallback always 1.0 for non-subsampled status).
Task 5: review 1 (opus) -> spec ✅; Important (plan-mandated): split-halves flags mostly healthy states (1σ drift in one of 9 -> 5 flagged; both endpoints + global-solve leakage); Important: missing-file / all-NaN paths return None without WARNING.
Ruling 6: replace edge-based split-halves with a per-state LOCAL test — each state's own reduced-bias mean, first vs second half of its own rows, Welch z, Bonferroni over tested states, same effect floor (kT) — cannot leak to other states and drops the two half MBAR solves (cheaper) — cost if wrong: less sensitive to a drift that changes f_k without changing the own-window bias distribution (unlikely: bias = f(CV) in that window).
Ruling 7: WARNING on every None return; partial warm start (missing f_init entries filled, not all-or-nothing); _local_sigma fallback = min over the row (nearest finite), not median; drop unused n_k param; exc_info on the catch-all warning; tests: specificity, warm-start kwargs spy, unsampled-endpoint edge omitted, non-contiguous ids, warning logged — cost if wrong: none.
Task 5: minor (deferred): cross-source row order relies on sorted source dirs (fine with <=1 topup per run root, Task 8 guarantees one); memory ~N×K temporaries (same as existing rung-overlap path).
Task 5: fix round 1/5 (commits 998e34e..2432e1f)
Task 5: fix round 1/5 re-review -> all addressed, 0 open
Task 5: complete (commits dfd35bf..2432e1f, review clean)
Task 6: dispatched (base 2432e1f, haiku)
Task 6: review 1 (opus) -> spec ✅; Important (plan-mandated): benefit/cost score is concave -> always shortest candidate L (measured: severe σ 0.30 gets 2,500 steps, needs 400,000); Important (plan-mandated): weak-edge structural/noise classification depends on id order (deficits mutated inside the edge loop).
Ruling 8: L = min(max over deficits of required(s), budget_max), rounded to interval; < interval -> cap_too_small; benefit/score candidate loop removed — spec 4.3 "deficit-driven, capped": the top-up exists to bring the worst deficit to target, one top-up per phase so a short L wastes the phase — cost if wrong: a patch with one very slow state runs longer (bounded by 4x-current-steps cap and the 0.3 budget cap).
Ruling 9: classify every weak edge against the frozen initial deficit set, then add noise-edge endpoints — brief's own rule "structural if both endpoints adequate" — cost if wrong: none.
Ruling 10: minors fixed in round 1: 3-rung layout-edge fallback test; exact predicted-σ assertion in the partial test (and upper clamp 2.0 exercised); _round_up subtracts 1e-9 relative tolerance before ceil; comment the σ/√2 target for noise-edge endpoints. Report inaccuracy (constants/concerns) noted.
Task 6: fix round 1/5 (commits b2ca3bb..e5049a8)
Task 6: fix round 1 re-review -> all addressed; gap: no multi-severity test for Ruling 8 -> round 2 (tests only)
Task 6: fix round 2/5 (tests only, verified by controller: worst-deficit test fails on old code; commits e5049a8..6accc62)
Task 6: complete (commits 2432e1f..6accc62, review clean)
Task 7: dispatched (base 6accc62)
Task 7: review 1 (sonnet) -> spec ✅; Important (plan-mandated): rung branch reads topup_weak_overlap instead of the calibrated min_rung_overlap used by every other rung reader; Important: 4 more live readers keep "overlap None = weak" (evaluate_adaptive_quality_gate non-rung branch, propose_actions_from_diagnostics, write_epoch_action_report, _adaptive_production_converged).
Ruling 11: rung branch uses policy.min_rung_overlap; test with divergent min_rung_overlap/topup_weak_overlap — one rung-weakness threshold per campaign — cost if wrong: none (both default 0.15).
Ruling 12: route the non-rung weak test of all 4 readers through _edge_is_measured_weak, keeping each reader's own rung handling — spec invariant "an unmeasured edge is never weak"; reports must agree with the gate; the final quality gate otherwise spends MD on an unmeasured number — cost if wrong: an edge unmeasured because an endpoint is unsampled no longer triggers a bridge/extension by itself (coverage stays owned by the min-samples checks).
Task 7: fix round 1/5 (commits 3f79a5c..30995d2): rulings 11/12 applied; write_epoch_action_report + _adaptive_production_converged kept "rung always weak" (overlap None) behind an edge_type guard.
Ruling 13: route rung edges in those two functions through _edge_is_measured_weak too — the "always weak" rung reading is the same unmeasured-is-weak bug (converged_by_current_policy can never be true on a ladder run; report disagrees with the gate) — cost if wrong: report/summary flag changes on ladder runs only, no MD decision.
Parked: tests/test_atlas_md_docs.py 2 failures (ATLAS-MD vs ATLaS-MD branding, missing mermaid block in docs/index.md) pre-exist this branch (files untouched since 4ea0814) — Task 11 (docs) looks at them.
Task 7: fix round 2/5 (commits 30995d2..28b7b4a)
Task 7: re-review -> findings 1,2 addressed (RED on 3f79a5c verified by reviewer); Minor open: converse test (measured-weak rung edge blocks _adaptive_production_converged) + direct write_epoch_action_report weak_edges tests.
Task 7: minor (deferred to final fix wave): above test gap; _adaptive_production_converged ignores pending "add_rung" actions (only "add"); gate uses _weak_rung_edge_reason while reports use the bare threshold (possible disagreement, unconfirmed).
Task 7: complete (commits 6accc62..28b7b4a)
Task 9: dispatched (base 28b7b4a, sonnet) — Ruling 1 order
Task 9: implementer DONE_WITH_CONCERNS (commits 28b7b4a..db76ee0); concern for Task 8: parent dirs ordered by index.json mtime (_topup_parent_dirs_by_creation_order), driver must not name-sort
Task 9: review 1 (opus) -> spec ✅ (2 flagged deviations: finite-CV1 raise OK; mtime ordering); Important: corrupt/partial export raises non-SeedMismatchError (campaign stop); Important (plan gap): seed check proves XML/index self-consistency only, cannot catch window->state mis-map (identity fallback); Important: mtime ordering not preserved by _copy_atomic/copy2/cp -> stale parent + same integrator seed can replay a trajectory.
Ruling 14: per-state XML written tmp+replace; load_seed_index/load_seed_states treat an unreadable/incomplete entry as missing (WARNING) -> surfaces as the existing missing-seed SeedMismatchError — binding "seeding never stops the campaign" — cost if wrong: none.
Ruling 15: export records per-window restraint (primary_center, primary_k, secondary_center, secondary_k) in index.json; seeding asserts they match the top-up's own window table for w (rel tol 1e-6); is_topup with no epoch_window_map.csv fails closed (SeedMismatchError) — closes the chignolin_6 mis-map class — cost if wrong: an old export without restraint fields can't seed (none exist yet; feature unreleased).
Ruling 16: ordering key = content, not mtime: index.json carries "export_seq" = time.time_ns() at export (survives copies); parents sorted by it; mtime only as fallback for a missing key — cost if wrong: clock skew between nodes (segments of one campaign export minutes-hours apart; skew << that).
Ruling 17: minors in same round: mismatch errors name window + state_id (attributes); seeded windows skip --randomize-replica-velocities (log once); SeedMismatchError during replica setup shuts down _sim_pool and releases the run lock before re-raising; Reference test asserts positions/velocities equality; move _state_id_of_window_from_epoch_map/_topup_parent_dirs_by_creation_order into gareus/topup_seeding.py; delete _augment_seed_bank_with_campaign_search if it has no remaining caller (constraint: old re-pull seeding removed).
Ruling 18: spec says a window failing the seed check is "dropped from that top-up"; implementation: a MISSING seed is dropped pre-launch by the driver (_seedable_patch, Task 8), a runtime CV/restraint mismatch ends that top-up (campaign continues) — dropping a replica after contexts are built means a rebuild for a should-never-happen event — cost if wrong: one lost top-up in a rare event. Record in Task 11 docs.
Task 9: fix round 1/5 (commits db76ee0..df347f2)
Task 9: re-review 1 -> Rulings 14-16 addressed; open: _augment_seed_bank_with_campaign_search still present (only call is a guaranteed no-op, guard reads is_topup on the non-top-up branch). New minors: whole-index corruption of newest parent silently falls back to an older parent's copy; randomisation skip logs per window, not once; run_gareus Ruling-17 paths untested.
Ruling 19: an unreadable index.json in any candidate parent raises SeedMismatchError from load_seed_index (fail closed: the top-up ends, campaign continues) — a stale-parent fallback restarts a chain from an earlier point, the replay class Ruling 16 closed — cost if wrong: one lost top-up on a corrupt file.
Task 9: minor (deferred): run_gareus Ruling-17 paths (randomisation skip, lock release, pool shutdown) have no test — needs a full run_gareus harness; final review to judge.
Task 9: fix round 2/5 (commits df347f2..6f3f141), controller-verified (deleted helpers have no callers; 36 pass)
Task 9: complete (commits 28b7b4a..6f3f141)
Task 9: minor (deferred): gareus/seeding.py _topup_auto_allow branch now unreachable (top-ups never pull) — Task 8 decides; io.py could export release_run_lock.
Task 8: dispatched (base 6f3f141, opus); carried: shared parent-dir helper for driver+segment, Ruling 18/19 handling, one top-up per phase across resume, Task-2 log wording, topups-off behaviour statement
Task 8: implementer DONE_WITH_CONCERNS (commits 6f3f141..4757c70): topups-off baseline now uniform share (new-state 2x bump gone), 2 tests changed, rung mbar_overlap reaches per-epoch gate when topups on
aurum2: job 2645611 walltime resubmit at epoch_002/baseline 48% (536750/1118000) 14:02; resume job 2647937 pending (start ~17:23); boundary est. ~21:30 — env fix NOT applied (mid-epoch)
Task 8: review 1 (opus) -> spec ✅ (budget-honouring deviation judged correct; 2 test re-statements legitimate; 2x new-state bump removal mandated by D2); Important: pool-skipped/clipped top-up recorded completed and calibrated vs full plan.steps (halves persisted correction).
Ruling 20: run_segment's outcome (actual steps, skipped_by_runtime_pool) reaches _run_phase_topup; skip -> plan reason "pool_exhausted", no calibration, no wall-time row; clip -> calibrate against predicted_sigma recomputed at the actual steps, wall-time row logs actual steps — correction must measure the gain model, not the pool — cost if wrong: none.
Ruling 21: minors in round 1: persist per-phase edge_overlap (topup_union_overlap.json) and reload on resume so gate/proposer see the same overlaps; a layout change after a partial topup_001_* with a checkpoint does not replan (reason "layout_changed", partial samples kept, no second top-up dir); crash-resume fast path omits realised_h; direct dirs-only test for topup_parent_dirs_by_creation_order; seed-mismatch test asserts saved reason; test that the top-up segment writes epoch_window_map.csv; non-default removed-score policy fields (low_sample_bonus, weak_edge_bonus, frontier_bonus, high_boost_bonus, new_state_steps) warn once as ignored.
Task 8: minor (deferred): _topup_auto_allow unreachable (not self-contained) + _articulation_is_degenerate test-only -> final review; plan.structural_edges unread + spatial structural edges reach bridges only via CV overlap -> final review vs spec 4.2; union cost at 236 states unmeasured -> Task 10 records it; CLAUDE.md "rung gap caught at campaign end" no longer holds with topups on -> Task 11.
Task 8: fix round 1/5 (commits 4757c70..ccfae6f); touched Task 6 (predicted_sigma module fn, sample_scale_steps in plan)
Task 8: re-review -> all addressed, 0 open
Task 8: minor (deferred): crash between clipped top-up end and topup_state save -> resume clip reads 0 -> recorded pool_exhausted (one lost calibration update, data kept); no test of real plan_topup output through clip re-prediction / sample_scale_steps save-load round-trip; empty topup_001_* dir from a pre-checkpoint crash lets a replan open a second dir.
Task 8: complete (commits 6f3f141..ccfae6f)
Task 10: dispatched (base ccfae6f, opus); + union cost measurement at 236 states
Task 10: implementer NEEDS_CONTEXT (commit af1a40e): B == A everywhere. Cause 1: union sqrt(O_ij O_ji) diluted ~1/K (K identical states -> 1/K; pairs 0.26-0.32 isolated vs 0.02-0.09 in 112-state union) -> ~all edges structural. Cause 2: sigma_k = min over neighbours is always won by a near-identical rung twin (median 0.007 vs target 0.10). Budget 5 h leaves every sigma 4-15x under target (regime vacuous). Union cost at 236 states: 1M rows 102.7 s / 14.6 GB RSS; 250k rows 26.4 s / 4.06 GB.
Ruling 22 (reopens Task 5): edge overlap = pairwise overlap with union f fixed, restricted to the pair's samples and a 2-state denominator; report sqrt(O_ij O_ji); exact test: same pair in 3- and 30-state unions gives identical values. Scale note: identical states -> 0.5 (not 1/K); topup_weak_overlap 0.15 and min_rung_overlap now read on the pairwise scale for top-up-path overlaps — cost if wrong: thresholds need recalibration on the new scale (isolated pairs read 0.26-0.32, 0.15 plausibly still separates).
Ruling 23: sigma_k rule chosen empirically in the harness: per state compute (i) min over all edge neighbours, (ii) min over same-rung spatial neighbours (rung fallback if none), (iii) current; correlate each with that centre's true local PMF error from reference_pmf; adopt the best; unit test for a sparse state with a well-sampled rung twin — cost if wrong: none (measured).
Ruling 24: study regime fixed from arm A only: per landscape the budget at which arm A max sigma ~1.5-2x target, frozen and recorded in the study JSON before arm B runs; wins need a strict margin (>1 %); missing-bridge test two-sided (routed with gap, not without); PMF RMSE + its Wilcoxon p is a co-criterion. If B still does not beat A on PMF RMSE, that is the result: report it, stop tuning.
Ruling 25: campaign-end rung overlap (mbar_state_overlap on the full union) likely has the same dilution — check on real union data first (read-only agent); if confirmed, tell the user immediately; any fix to the campaign-end path is a separate vetoable commit, not part of Task 5.
Task 5: reopened (Ruling 22), done by the Task 10 implementer as a separate commit with its own review.
Ruling 25 check: CONFIRMED on RUNS/chignolin_7 union (K=64, 16 centres x 4 rungs, 2.39M finite rows): 48 adjacent-rung pairs full-union sqrt(O_ij O_ji) median 0.0887 (0.065-0.214) vs pairwise median 0.2579 (0.197-0.428), ratio median 2.71; below 0.15: 38/48 full vs 0/48 pairwise. Call sites all full-union: adaptive_production.rung_mbar_overlap_from_union (:3765, campaign-end gate/add_rung), union_diagnostics (:154, top-ups), analyze_gareus_mbar.py:5377 -> ladder_overlap health verdict. Table: /home/sulcjo/.claude/jobs/afcc2200/tmp/dilution/rung_pair_table.csv
Ruling 26: after Task 10, a separate commit "fix: pairwise rung overlap in campaign-end gate and ladder health" switches call sites 1 and 3 to the shared pairwise function (Ruling 22's), user can veto; not deployed to aurum2 without the user.
aurum2 env fix DONE 17:15: epoch_002->final boundary reached 17:09 (job 2647937); pip uninstall jax 0.3.0 + jaxlib 0.1.75 from calc env; pymbar 4.0.1 imports, MBAR/detect_equilibration OK; deployed gareus equilibrated_subsample -> status subsampled. Graceful stop NOT issued: final schedule already persisted (resume reuses it), pymbar-dependent steps run at campaign end, walltime resubmit (~19:45) restarts with fixed env. Revert: pip install jax==0.3.0 jaxlib==0.1.75
Task 10 + Task 5 reopen: implementer DONE_WITH_CONCERNS (commits ccfae6f..f1e6a09: af1a40e harness, 9858046 pairwise overlap, 08f38cc sigma_k = max over same-rung spatial neighbours [Spearman vs true local dF error: pooled 0.791 vs old 0.729], f1e6a09 frozen-regime study). 20-seed verdict: B beats A on PMF RMSE nowhere; max sigma PASS only gated-barrier (20/20, p=9.5e-7) but RMSE 9/20 p=0.41; rugged-2d + slow-cv2 FAIL both; harmonic-bowl PASS; two-sided bridge FAIL (intact ridge edges also < 0.15 pairwise). 3 A/B tests fail as written. Per Ruling 24: result reported, tuning stopped.
Task 10: review 1 (opus) -> production Rulings 22/23 correct; verdict NOT trustworthy: every state deficient on 3/4 landscapes (B == whole-ladder extension split 70/30); verified biases vs B: 0.7x epoch-0 baseline leaves 16 states with 0 rows (invisible to allocator), patch tau penalty g x1.25; bridge comparison keyed by shifted state indices (104 vs 112 states); "epochs_topping_a_routed_edge==0" tautological; ~5 decorrelated rows/state so split-halves never fires.
Ruling 27: bridge test keyed by (centre coords, lambda); assert edges flanking the removed pass are routed with the gap and absent (by coordinate) from the intact layout; no "nothing routed intact" assertion; routing metric = edges topped in an earlier epoch that end structural (report the count).
Ruling 28 (supersedes Ruling 24's max-sigma band): regime chosen from arm A only by deficit fraction — budget where 10-40 % of states are above target in arm A's last epoch AND median >= 40 decorrelated rows per state per campaign (split-halves can fire); frozen and recorded before arm B; if no budget satisfies both on a landscape, report that landscape as "no targetable regime" rather than testing it.
Ruling 29: patch tau penalty (fewer exchange partners in a sparse patch) is kept — it models a real cost of lockstep patches; the verdict must state it; the allocator's gain model ignoring it is a production concern for the final review/Task 11 (correction factor may absorb it).
Ruling 30: max-rule x structural-route interaction (a true gap keeps both endpoints deficient, costs max_edge_attempts top-ups before routing) -> final whole-branch review; not changed now.
Ruling 31: this is the LAST study round: after these harness fixes the result is accepted as is; any A/B test still failing gets xfail(strict=True, reason citing the study JSON + this ruling). Study outputs: commit per-landscape summaries + regenerate commands; per-seed/per-epoch detail gzipped or omitted.
Task 10: final round (commits f1e6a09..35cfacb): Ruling 28 -> NO landscape has a targetable regime (at the 40-rows floor, 2.0-2.5 wall-h, no arm-A state above target) -> spec 6.2 heterogeneous criterion untestable in this harness. Informational at rows floor (20 seeds, 0 zero-row states): max sigma B better gated-barrier -8.4 % (20/20, p=9.5e-7), slow-cv2 -1.3 % (19/20); PMF RMSE better nowhere (gated-barrier +7 % worse, 7/20). Bridge two-sided PASS 20/20; 5-7 edges/seed topped as noise before routed. g(patch)/g(all) 1.26 / 1.19. 108 passed, 1 xfail(strict). Accepted per Ruling 31.
Ruling 26: dispatched (base 35cfacb, sonnet): shared pairwise_state_overlap in mbar_analysis/ladder.py; campaign-end rung gate + ladder health switched; separate vetoable commit
Task 10: final-round re-review -> all addressed, 0 open
Task 10: minor (deferred): ArmResult.structural_routed dead field
Task 10: complete (commits ccfae6f..35cfacb; incl. Task 5 reopen 9858046, 08f38cc)
Ruling 26: implementer DONE (commit 35cfacb..f7e007b), 73 passed
Ruling 26: review (opus) -> Approved; real c7 check reproduces pairwise table (median 0.25788, 0/48 < 0.15, max diff 7e-16); ~21 ms/edge; production health path is pairwise.
Ruling 26: minor (deferred to final wave): calibration comments wrongly say the S3 pilot was "~4-state, effectively pairwise" (it was 5-state full-matrix, also diluted; pairwise thresholds not re-measured); stale _symmetric_state_overlap docstring (dead in production); ladder_overlap pair_overlap=None default grades full-matrix vs pairwise thresholds (footgun); no test pins the analyze_gareus_mbar.py:5392 wiring.
Ruling 26: complete (commit f7e007b)
Task 11: dispatched (base f7e007b, sonnet)
Task 11: implementer DONE (f7e007b..5b2a74f); stale --ap-topups argparse help ('fresh pull') -> final wave
Task 11: review 1 (sonnet) -> factual checks OK; 8 Important doc inaccuracies: no ap_topups:true results-changing note; old lambda-ladder CLAUDE.md section left stale (mbar_state_overlap, "caught at campaign end"); missing-vs-mismatch seed handling misstated (Ruling 18); cap_too_small cause inverted (budget < 1 interval, not required L); 4x current-steps cap on required L undisclosed; baseline shortened by topup_max_fraction for every state when top-ups on undisclosed; "topped as structural" wording; topups-todo T1 gate unsatisfiable. (Reviewer's "400k capped at 2,500" misreads Ruling 8; the 4x cap itself is real.)
Ruling 32: fix all 8 + minors + cli.py --ap-topups help text (5th file allowed); T1 gate rewritten: chignolin_10 proceeds as the real-MD test (user decision 2026-09-24), not gated on the synthetic pass.
Task 11: fix round 1/5 (5b2a74f..4078f68), 7 passed
Task 11: controller check: no stale "fresh pull"/"topped as structural" left (adaptive_production.py:338 comment -> final review)
Task 11: complete (commits f7e007b..4078f68)
Final review: dispatched (base 4ea0814..4078f68, opus)
Final review (opus): With fixes. c9 redeploy (top-ups off): resumed final baseline length identical, subsampling identical, gate changes logging-only, f7e007b flips rung + cv1_direction ladder verdicts toward pass (c7: 38/48->0/48, 46/60->0/60). Important: I1 final_window_states export runs for every campaign (~0.74 GB/236-window segment); I2 top-ups-on resume of an old-code schedule takes full_steps = min baseline; I3 final phase strands the withheld fraction when its top-up does not run; I4 Ruling 30 worse: structural-edge endpoints stay deficient every phase; I5 sigma-setting neighbour not guaranteed in patch; I6 calibration comments claim pairwise-calibrated thresholds. Minor M1-M9.
Ruling 33: one fix wave for I1-I6 + M1, M2 (log structural edges), M3, M5 (delete dead code + its tests), M6 (warn), union memory guard (rows x K pre-check -> no_diagnostics); M4, M7, M8, M9 accepted/documented.
DEPLOY 2026-09-25 20:40: pairwise overlap fix only (branch deploy/pairwise-overlap = e74dd30 + cherry-pick f7e007b minus top-up-only union_diagnostics.py -> dda8caa) rsynced to both aurum2 trees + analyze_gareus_mbar.py; DEPLOYED_COMMIT dda8caa; md5 trees identical; 55 targeted tests passed; running c9 job keeps old code in memory, next resubmit loads it
MERGED: PR #103 (deploy/pairwise-overlap -> main, merge 90b7eba), CI docs+fast-correctness pass
Final fix wave: DONE_WITH_CONCERNS (commits 4078f68..3b525e3, 202 passed + 1 xfail); concerns: memory guard 8 GB admits ~550k kept rows at 236 states (c9-size would skip diagnostics); I3 extension asks pool clip for full target not delta; I3 adds full withheld fraction after a partial top-up; I1 tested by predicate+AST only
Final fix wave re-review (opus): all addressed except I3 (Important): final-phase extension is a no-op under an enabled MD pool (run_segment clips the full target against the remaining pool -> clip < checkpointed -> already-complete fast path; reproduced with a real AdaptiveRuntimePool: 20000->5999, 0.072/0.24 ns stranded). Residual minors: duplicate already_complete row/misleading print; exact-exhaustion path marks all states skipped_by_runtime_pool; summary baseline_steps shows shortened length. Memory guard 8 GB judged sensible (c9-size ~686k raw rows campaign-wide).
Ruling 34 (residual adjudication): fix I3 now — under force_resume only, clip the DELTA: actual = prior + clip_steps(n, requested - prior); ordinary resumes (chignolin_9) unchanged; test with a real AdaptiveRuntimePool; exact-exhaustion + force_resume must not mark states skipped (extension simply no-ops with one log line); summary baseline_steps reports the extended length. Controller verifies tests; no further review round.
I3 residual: fixed 27d6625, controller-verified (152 passed; RED 2 failed on old code); branch complete
